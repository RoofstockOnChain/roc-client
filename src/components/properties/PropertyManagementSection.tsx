import { FC } from 'react';
import { PropertyManagement } from '../../models/PropertyManagement';

interface PropertyManagementSectionProps {
  propertyManagers: PropertyManagement[];
}

export const PropertyManagementSection: FC<PropertyManagementSectionProps> = ({
  propertyManagers,
}) => {
  return <></>;
};
